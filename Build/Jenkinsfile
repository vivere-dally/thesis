pipeline {
    agent {
        label "master"
    }

    options {
        copyArtifactPermission '*'
    }

    stages {
        stage("Test") {
            steps {
                echo "Hello world"
            }
        }
    }
}