version: '3.8'
services:
  thesis:
    # [production-tag] will be replaced in the Jenkins Build Job
    image: thesis:[production-tag]
    container_name: thesis
    restart: always
    build:
      context: ./../
      dockerfile: ./docker/Dockerfile
    ports:
      # Azure Web App exposes port 80
      - 80:5000
